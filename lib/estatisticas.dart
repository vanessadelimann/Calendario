import 'package:flutter/material.dart';

class Estatisticas extends StatelessWidget {
  const Estatisticas({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Estatisticas");
  }
}
